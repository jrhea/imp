language: rust
os: osx
script:
  - cargo build --verbose --all
  - cargo test --verbose --all
  - cargo fmt

rust:
  - stable
  - beta
matrix:
  allow_failures:
    install:
      -rustup component add rustfmt
