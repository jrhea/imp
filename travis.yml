language: rust
script:
  - cargo build --verbose --all
  - cargo test --verbose --all
  - cargo fmt

rust:
  - stable
  - beta
matrix:
  allow_failures:
    fast_finish: true
    install:
      -rustup component add rustfmt
